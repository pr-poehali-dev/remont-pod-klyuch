# 🤖 Инструкция по настройке и тестированию Telegram бота

## ✅ Что уже сделано

1. ✅ Функции бота задеплоены:
   - `telegram-bot` (обработка webhook): `https://functions.poehali.dev/d3d9e204-ba60-493c-aa58-838dc61d8592`
   - `telegram-auth` (API авторизации): `https://functions.poehali.dev/307cdc97-68e5-41cf-a3bc-7744698eb7c1`

2. ✅ База данных подготовлена:
   - Таблица `users` (для пользователей)
   - Таблица `telegram_auth_tokens` (для временных токенов)
   - Таблица `refresh_tokens` (для обновления сессий)

3. ✅ Секреты добавлены в проект (нужно заполнить значения):
   - `JWT_SECRET` ✅ (заполнен)
   - `TELEGRAM_BOT_TOKEN` ⚠️ (нужно получить от @BotFather)
   - `TELEGRAM_BOT_USERNAME` ⚠️ (username бота без @)
   - `SITE_URL` ✅ (http://bk-63.ru)
   - `TELEGRAM_WEBHOOK_SECRET` ⚠️ (любая случайная строка)

4. ✅ Тестовая страница создана: `/telegram-test`

---

## 📋 Шаги для запуска

### Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду: `/newbot`
3. Придумайте название бота (например: "Ремонт под ключ бот")
4. Придумайте username для бота (например: `remont_pod_klyuch_bot`)
5. BotFather выдаст вам **токен бота** вида: `1234567890:ABCdefGHI_jklmnop...`

### Шаг 2: Добавьте токен в секреты

Скопируйте токен из BotFather и вставьте в поле `TELEGRAM_BOT_TOKEN` в секретах проекта.

### Шаг 3: Добавьте username бота

Укажите username бота БЕЗ символа @ в поле `TELEGRAM_BOT_USERNAME`.
Например: `remont_pod_klyuch_bot`

### Шаг 4: Создайте webhook secret

Придумайте любую случайную строку (или используйте эту):
```
webhook_secret_remont_2026_xyz123
```

Добавьте её в поле `TELEGRAM_WEBHOOK_SECRET`.

### Шаг 5: Зарегистрируйте webhook

Скопируйте эту ссылку и **замените значения** в фигурных скобках:

```
https://api.telegram.org/bot{ВАШ_TELEGRAM_BOT_TOKEN}/setWebhook?url=https://functions.poehali.dev/d3d9e204-ba60-493c-aa58-838dc61d8592&secret_token={ВАШ_TELEGRAM_WEBHOOK_SECRET}
```

**Пример готовой ссылки:**
```
https://api.telegram.org/bot1234567890:ABCdefGHI_jklmnop/setWebhook?url=https://functions.poehali.dev/d3d9e204-ba60-493c-aa58-838dc61d8592&secret_token=webhook_secret_remont_2026_xyz123
```

Откройте эту ссылку в браузере. Вы должны увидеть:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

---

## 🧪 Тестирование

### 1. Откройте тестовую страницу

Перейдите на: `http://bk-63.ru/telegram-test`

### 2. Нажмите кнопку "Войти через Telegram"

Должен открыться ваш Telegram бот.

### 3. Отправьте команду в боте

В чате с ботом отправьте:
```
/start web_auth
```

### 4. Получите ссылку от бота

Бот должен ответить сообщением:
```
Авторизация готова!

Нажмите кнопку ниже, чтобы войти на сайт 👇

Ссылка действительна 5 минут
```

И покажет кнопку **"Войти на сайт"**.

### 5. Нажмите кнопку

Вас перенаправит на сайт, и вы увидите сообщение **"✅ Авторизация успешна!"**

---

## 🐛 Проверка ошибок

### Проверка 1: Webhook настроен?

Откройте в браузере:
```
https://api.telegram.org/bot{ВАШ_TELEGRAM_BOT_TOKEN}/getWebhookInfo
```

Вы должны увидеть:
```json
{
  "ok": true,
  "result": {
    "url": "https://functions.poehali.dev/d3d9e204-ba60-493c-aa58-838dc61d8592",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Проверка 2: Бот отвечает?

Откройте бота в Telegram и отправьте:
```
/start
```

Бот должен ответить:
```
Привет! Используйте кнопку «Войти через Telegram» на сайте.
```

### Проверка 3: Токены в базе

После команды `/start web_auth` проверьте, что токен сохранился:
```sql
SELECT * FROM telegram_auth_tokens ORDER BY created_at DESC LIMIT 1;
```

Должна появиться новая запись с вашим `telegram_id`.

---

## 📊 Структура потока авторизации

```
1. Пользователь нажимает "Войти через Telegram" на сайте
   ↓
2. Открывается бот в Telegram (t.me/{bot_username}?start=web_auth)
   ↓
3. Пользователь отправляет /start web_auth
   ↓
4. Бот создаёт временный токен и сохраняет в БД
   ↓
5. Бот показывает кнопку со ссылкой: {SITE_URL}/auth/telegram/callback?token=xxx
   ↓
6. Пользователь нажимает кнопку → попадает на страницу callback
   ↓
7. Страница отправляет token на функцию telegram-auth
   ↓
8. Функция проверяет токен, создаёт пользователя (если новый)
   ↓
9. Функция возвращает access_token и refresh_token
   ↓
10. Страница сохраняет токены в localStorage
   ↓
11. Редирект на /telegram-test → пользователь видит свои данные ✅
```

---

## 🎯 Ожидаемый результат

После успешной настройки:
- ✅ Бот отвечает на `/start`
- ✅ Бот создаёт токены на `/start web_auth`
- ✅ Кнопка перенаправляет на сайт
- ✅ Авторизация работает
- ✅ Данные пользователя отображаются на `/telegram-test`

---

## 🆘 Помощь

Если что-то не работает:
1. Проверьте все секреты в проекте
2. Убедитесь, что webhook настроен (`getWebhookInfo`)
3. Проверьте логи функций в poehali.dev
4. Напишите в чат — помогу разобраться!