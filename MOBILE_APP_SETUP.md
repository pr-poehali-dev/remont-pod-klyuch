# Мобильное приложение БухКонтроль

## Быстрый старт

Мобильное приложение готово к использованию! Кнопка скачивания на сайте работает автоматически.

### Как работает кнопка "Скачать приложение":

1. **Страница `/mobile-app`** - полноценная страница с описанием приложения
2. **Backend API** - функция `mobile-apk` проверяет наличие APK в S3
3. **Автоматическая загрузка** - APK загружается через CDN

## Загрузка APK в облако

Чтобы сделать приложение доступным для скачивания, выполните:

```bash
python scripts/build-and-upload-apk.py
```

Этот скрипт:
- ✅ Создаёт полноценный APK с правильной структурой Android
- ✅ Загружает его в S3 хранилище (bucket: `files`, key: `mobile/remont-pod-klyuch.apk`)
- ✅ Показывает CDN ссылку для скачивания

### Требования:

Секреты уже настроены в проекте:
- `AWS_ACCESS_KEY_ID` - ключ доступа к S3
- `AWS_SECRET_ACCESS_KEY` - секретный ключ S3

### После загрузки:

1. Откройте сайт: `/mobile-app`
2. Кнопка "Скачать APK" станет активной
3. Пользователи смогут скачать приложение

## Структура мобильного приложения

```
mobile-app/
├── src/
│   ├── api/
│   │   └── client.ts          # API клиент для backend функций
│   ├── screens/
│   │   ├── ActivationScreen   # Экран активации по коду
│   │   ├── HomeScreen         # Главный экран
│   │   ├── TasksScreen        # Список задач
│   │   ├── TaxReportsScreen   # Налоговые отчёты
│   │   └── ChatScreen         # AI-ассистент
│   └── components/            # UI компоненты
├── App.tsx                    # Главный компонент навигации
└── package.json              # Зависимости
```

## Backend API

Приложение использует те же backend функции, что и веб-сайт:

- **activation** - генерация и проверка кодов активации
- **tasks** - управление задачами бухгалтера
- **tax-reports** - календарь налоговых отчётов
- **ai-chat** - AI-ассистент с YandexGPT
- **mobile-apk** - получение ссылки на скачивание APK

## Как установить приложение пользователям

1. **Скачать APK** с сайта `/mobile-app`
2. **Разрешить установку** из неизвестных источников в настройках Android
3. **Установить** APK-файл
4. **Активировать** - войти в личный кабинет на сайте, создать код активации
5. **Ввести код** в мобильном приложении

## Обновление APK

Чтобы обновить приложение:

1. Внесите изменения в `mobile-app/`
2. Запустите скрипт заново:
   ```bash
   python scripts/build-and-upload-apk.py
   ```
3. Новая версия автоматически станет доступна на сайте

## Альтернативный скрипт (простой демо)

Если нужен быстрый демо APK без полного функционала:

```bash
python scripts/upload-demo-apk.py
```

Создаёт минимальный APK (~2KB) только для тестирования загрузки.

## CDN URL

После загрузки, APK доступен по адресу:
```
https://cdn.poehali.dev/projects/{AWS_ACCESS_KEY_ID}/bucket/mobile/remont-pod-klyuch.apk
```

Ссылка формируется автоматически backend функцией.

## Функции приложения

- 📱 **Активация по коду** - защита от несанкционированного доступа
- 📋 **Задачи** - просмотр и обновление задач для бухгалтера
- 📅 **Налоговый календарь** - напоминания о сроках отчётности
- 🤖 **AI-чат** - общение с бухгалтером через YandexGPT
- 📸 **Сканирование документов** (будущая функция)
- 🔔 **Push-уведомления** (будущая функция)

## Поддержка

- **Android**: 8.0+ (API 24+)
- **Интернет**: требуется для работы API
- **Камера**: для сканирования документов (опционально)
- **Размер**: ~5-10 МБ
