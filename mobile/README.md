# Мобильное приложение "Ремонт под ключ"

Мобильное приложение на React Native для работы с бухгалтерскими документами, задачами и календарём налоговых отчётов.

## 🚀 Возможности

- **Сканирование документов** - Фото первичных документов (чеки, счета) с автоматической загрузкой в облако
- **Задачи бухгалтеру** - Постановка задач (формирование счетов, консультации и т.д.)
- **Календарь отчётов ФНС** - Отслеживание сроков подачи налоговых деклараций с напоминаниями

## 📋 Требования

- Node.js 18+
- npm или yarn
- Expo CLI
- Для iOS: Xcode (macOS)
- Для Android: Android Studio

## 🛠 Установка

1. Установите зависимости:
```bash
cd mobile
npm install
```

2. Установите Expo CLI глобально (если ещё не установлен):
```bash
npm install -g expo-cli
```

## 🏃 Запуск

### Режим разработки с Expo Go

```bash
npm start
```

Отсканируйте QR-код в приложении Expo Go на вашем телефоне.

### Запуск на iOS симуляторе (macOS)

```bash
npm run ios
```

### Запуск на Android эмуляторе

```bash
npm run android
```

## 🔧 Структура проекта

```
mobile/
├── App.tsx                      # Главный файл приложения
├── src/
│   ├── screens/                 # Экраны приложения
│   │   ├── LoginScreen.tsx      # Вход через Telegram/Email
│   │   ├── DocumentScannerScreen.tsx  # Сканирование документов
│   │   ├── TasksScreen.tsx      # Управление задачами
│   │   └── TaxReportsScreen.tsx # Календарь отчётов ФНС
│   ├── api/
│   │   ├── config.ts            # URL endpoints backend функций
│   │   └── client.ts            # HTTP клиент с автоматическим добавлением токена
│   └── context/
│       └── AuthContext.ts       # Контекст авторизации
├── package.json
├── app.json                     # Конфигурация Expo
└── tsconfig.json
```

## 🔐 Авторизация

Приложение использует JWT токены для авторизации:
- Токены сохраняются в `SecureStore` (безопасное хранилище)
- Автоматически добавляются к каждому запросу в backend
- Реализована интеграция с Telegram авторизацией

## 🌐 Backend API

Приложение использует следующие Cloud Functions:

- **Documents API**: `https://functions.poehali.dev/fa6a8fa6-dc0d-4bf5-a4f3-b4635d425418`
  - POST / - загрузка документа (base64)
  - GET / - список документов

- **Tasks API**: `https://functions.poehali.dev/a29307b8-67a5-4868-91d2-61833462e14c`
  - POST / - создать задачу
  - GET / - список задач
  - PUT /?id=X - обновить задачу

- **Tax Reports API**: `https://functions.poehali.dev/2d73a910-182a-4f8c-9f82-1e4c985f07fa`
  - GET / - календарь отчётов
  - POST / - создать отчёт
  - PUT /?id=X - обновить статус

## 📱 Сборка для продакшена

### Android APK

```bash
expo build:android
```

### iOS IPA

```bash
expo build:ios
```

## 📝 Примечание

Это приложение работает отдельно от веб-версии сайта и использует те же backend функции. 
Для локального тестирования используйте Expo Go на реальном устройстве или эмуляторе.

## 🐛 Отладка

- Логи: `console.log` выводятся в терминал с `npm start`
- Для продвинутой отладки используйте React Native Debugger
- Ошибки сети смотрите в Network tab браузера при включенном Remote Debugging
